You are editing an existing Next.js 14 / TypeScript / Tailwind CSS / Framer Motion website 
for "Restez Chez Soi". Apply the following changes across ALL files. 
Do not rebuild from scratch — surgically edit only what is specified.

---

## CHANGE 1 — REMOVE HERO BADGE TEXT

File: components/HeroHome.tsx (or app/page.tsx if hero is inline)

Find and DELETE this element entirely:
The small pill/badge near the top of the hero that reads 
"Disponibles 7 jours/7 · Québec & Lévis" OR "Soins à domicile · Québec"
(any badge/pill element inside the hero section with that text)

Do not remove anything else in the hero. Keep the H1, subtitle, buttons, video, overlay.

---

## CHANGE 2 — REMOVE ALL SECTION BADGES SITEWIDE

A "section badge" is a small pill-shaped label that appears above section titles.
Examples of text they contain: "NOS SOINS", "NOTRE APPROCHE", "TÉMOIGNAGES", 
"NOS SERVICES", "CONTACT", "À PROPOS", "NOTRE MISSION", "NOS VALEURS", 
"ZONE DESSERVIE", "TÉMOIGNAGES", "PERSONNEL", "01 — Présence à domicile",
"02 — Soins infirmiers", "03 — Accompagnement", or any similar uppercase pill label.

These are typically rendered as one of:
- <SectionBadge> component
- <span className="...rounded-full...uppercase..."> 
- <div className="section-badge ...">
- Any element with classes like: bg-rose-light, bg-bleu-bg, rounded-full, 
  tracking-widest, uppercase, text-[11px] or text-xs used as a label above an h2

ACTION: Delete every instance of these badge elements across ALL pages and components:
- app/page.tsx
- app/services/page.tsx
- app/apropos/page.tsx
- app/contact/page.tsx
- components/PageHero.tsx (remove the badge prop rendering)
- components/SectionBadge.tsx (delete the file entirely)
- Any other component that renders a badge

Also update the PageHero component: remove the badge prop from the interface and 
remove its render. Update all PageHero usages to remove the badge prop.

Do NOT remove the H2 titles or subtitles that sit below the badges — only the badge itself.

---

## CHANGE 3 — REMOVE TESTIMONIALS SECTION COMPLETELY

Delete ALL testimonial-related content from every file:

Files to modify:
- app/page.tsx → remove the entire <TestimonialSlider /> section block
- app/apropos/page.tsx → remove the entire testimonials grid section
- components/TestimonialSlider.tsx → DELETE this file
- lib/data.ts → remove the Testimonial interface and the testimonials array export

In app/page.tsx: the section that contains TestimonialSlider sits between the 
About Strip and the Footer — remove the entire section tag wrapping it.

In app/apropos/page.tsx: the static 3-col testimonials grid section (bg-gris-light) 
— remove the entire section tag wrapping it.

Do not leave empty section tags, empty divs, or commented-out code. 
Remove cleanly so adjacent sections flow directly into each other.
